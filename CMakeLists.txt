CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(geticc)
FIND_PACKAGE(FLTK REQUIRED)
ADD_EXECUTABLE(geticc geticc.cxx)
INCLUDE_DIRECTORIES(${FLTK_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(geticc ${FLTK_LIBRARIES})
